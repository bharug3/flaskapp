---
- hosts: all
  become: yes
  gather_facts: false
  host_vars: ~/ec.py
  tasks:
  - yum: name=flask state=latest
  - shell: cd /
  - copy: src=/var/lib/jenkins/workspace/flaskapp/* dest=/home/ec2-user
